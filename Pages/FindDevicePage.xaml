<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:services="clr-namespace:PawfeedsProvisioner.Services"
             xmlns:models="clr-namespace:PawfeedsProvisioner.Models"
             x:Class="PawfeedsProvisioner.Pages.FindDevicePage"
             Title="Find Your Device">

    <Grid RowDefinitions="*, Auto">
        <ScrollView Grid.Row="0">
            <VerticalStackLayout Padding="20" Spacing="15">
                <Label 
                    Text="Searching for your Pawfeeds devices on the network..."
                    FontSize="16"
                    HorizontalOptions="Center" />

                <ActivityIndicator x:Name="Spinner" IsRunning="True" Color="{StaticResource Primary}" />

                <CollectionView x:Name="List">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:OnlineDeviceViewModel">
                            <Frame Padding="15" CornerRadius="10" BorderColor="{StaticResource Gray200}" HasShadow="False" Margin="0,5">
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer Tapped="OnDeviceTapped" />
                                </Frame.GestureRecognizers>
                                <Grid ColumnDefinitions="*, Auto" RowDefinitions="Auto, Auto, Auto, Auto">
                                    <!-- Device Display Name -->
                                    <Label Grid.Row="0" Grid.Column="0" 
                                           Text="{Binding DisplayName}" 
                                           FontSize="18" 
                                           FontAttributes="Bold" />

                                    <!-- Camera IP -->
                                    <Label Grid.Row="1" Grid.Column="0" FontSize="14">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Camera IP: " FontAttributes="Bold" />
                                                <Span Text="{Binding Ip}" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>

                                    <!-- Feeder IP -->
                                    <Label Grid.Row="2" Grid.Column="0" FontSize="14">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Feeder IP: " FontAttributes="Bold" />
                                                <Span Text="{Binding FeederIp}" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>

                                    <!-- Feeder Slot -->
                                    <Label Grid.Row="3" Grid.Column="0" FontSize="14">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Feeder Slot: " FontAttributes="Bold" />
                                                <Span Text="{Binding FeederId}" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>

                                    <!-- Chevron Icon -->
                                    <Image Grid.Row="0" Grid.RowSpan="4" Grid.Column="1" 
                                           Source="chevron_right.png" 
                                           HeightRequest="20" 
                                           Aspect="AspectFit" 
                                           VerticalOptions="Center" />
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>

        <VerticalStackLayout Grid.Row="1" Padding="20" Spacing="10">
            <Button Text="Rescan" Clicked="OnRescan" />
            <Button x:Name="DoneButton" Text="Done" IsVisible="False" Clicked="OnDone" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
